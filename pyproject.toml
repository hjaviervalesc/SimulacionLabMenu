[project]
name = "simulacion-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "taichi>=1.7.4",
]

[tool.uv.workspace]
members = [
    "SSDM",
]
